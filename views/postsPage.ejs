<!DOCTYPE html>
<html lang="en">

<head>
  <title>
    Finterest! <%= title %>
  </title>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
  <link rel="stylesheet" href="/vendor/border-box.css" />
  <link rel="stylesheet" href="/styles/layout.css" />
  <link rel="stylesheet" href="/styles/postsPage.css" />
  <link rel="stylesheet" href="/styles/header.css" />

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Roboto+Mono:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;1,400&display=swap"
    rel="stylesheet">

  <script src="/vendor/jquery-3.0.0.js"></script>
  <script defer src="/scripts/users.js"></script>

  <!-- <script>
    $(document).ready(function() {
      $('#check').click(function() {
        alert("The button was clicked.");
      });
    });
  </script> -->
</head>

<body>
  <%- include('partials/_header') %>
    <main class="container">
      <section class="left-section">
        <div id="title">
          <%= title %>
        </div>
        <img id="thumbnail" src="<%= image_url %>">
        <p>
          <%= description%>
        </p>
        <a href="<%= content_url %>">Visit content page</a>
        <p>
          <%= date_posted %>
        </p>
      </section>
      <section class="right-section">
        <div id="rating">
          <h3>Rate</h3>
        </div>
        <div id="comment">
          <!-- form action -->
          <form action="/" method="POST">
              <textarea id="comment-box" type="text" placeholder="Add a comment..."></textarea>
                <button id="comment-button" type="submit">Comment</button>
            </form>
            <h5>Comments</h5>
            <!-- if statement for comments -->
            <%= name %>:
            <%= content %>
          <!-- implement AJAX using JQuery for the comments ? if no time just send some type of jQuery form submit text-->
        </div>
      </section>
    </main>

    <!-- <button id="check">Check</button> -->
</body>

</html>
